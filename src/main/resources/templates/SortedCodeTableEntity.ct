package {package};

import java.sql.Timestamp;
import java.util.*;

import javax.persistence.*;

@Entity
@Table(name = "{tableName}")
public class {className} extends SortedCodeTableEntity {

{fields}
{getterSetters}
}